import { FreshnessDirective } from './freshness.directive';

describe('FreshnessDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshnessDirective();
    expect(directive).toBeTruthy();
  });
});
